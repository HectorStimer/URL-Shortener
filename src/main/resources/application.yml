spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/encurtador
    username: encurtador
    password: encurtador123
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
  redis:
    host: localhost
    port: 6379
  cache:
    type: redis

server:
  port: 8080
